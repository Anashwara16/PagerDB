Building PagerDB, a file-backed key-value store / mini storage engine in modern C++. I’m implementing a page-oriented storage manager, slotted-page record layout, and on-disk B+ tree indexing; future work includes adding write-ahead logging (WAL) and a buffer pool (page cache) to enable crash-safe recovery.
